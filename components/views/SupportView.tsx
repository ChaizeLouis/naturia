'use client'
export default function SupportView(props: any) {
  return <div style={{fontFamily:'Fraunces, serif',fontSize:'1.6rem',fontWeight:600,padding:'2rem',color:'var(--text)'}}>🌿 SupportView — En cours de chargement…</div>
}
