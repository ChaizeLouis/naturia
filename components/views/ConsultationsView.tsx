'use client'
export default function ConsultationsView(props: any) {
  return <div style={{fontFamily:'Fraunces, serif',fontSize:'1.6rem',fontWeight:600,padding:'2rem',color:'var(--text)'}}>🌿 ConsultationsView — En cours de chargement…</div>
}
